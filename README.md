# PixiJS Samples

To run the samples

Install the common dependencies

```sh
cd pixisamples
npm install
```

Go to the sample directory, and install the local dependencies

E.g.

```sh
cd pixi_01
npm install
npm run dev
```
